// Code generated by ent, DO NOT EDIT.

package ent

import (
	"common/ent/examresult"
	"fmt"
	"strings"

	"entgo.io/ent"
	"entgo.io/ent/dialect/sql"
)

// ExamResult is the model entity for the ExamResult schema.
type ExamResult struct {
	config
	// ID of the ent.
	ID           int `json:"id,omitempty"`
	selectValues sql.SelectValues
}

// scanValues returns the types for scanning values from sql.Rows.
func (*ExamResult) scanValues(columns []string) ([]any, error) {
	values := make([]any, len(columns))
	for i := range columns {
		switch columns[i] {
		case examresult.FieldID:
			values[i] = new(sql.NullInt64)
		default:
			values[i] = new(sql.UnknownType)
		}
	}
	return values, nil
}

// assignValues assigns the values that were returned from sql.Rows (after scanning)
// to the ExamResult fields.
func (er *ExamResult) assignValues(columns []string, values []any) error {
	if m, n := len(values), len(columns); m < n {
		return fmt.Errorf("mismatch number of scan values: %d != %d", m, n)
	}
	for i := range columns {
		switch columns[i] {
		case examresult.FieldID:
			value, ok := values[i].(*sql.NullInt64)
			if !ok {
				return fmt.Errorf("unexpected type %T for field id", value)
			}
			er.ID = int(value.Int64)
		default:
			er.selectValues.Set(columns[i], values[i])
		}
	}
	return nil
}

// Value returns the ent.Value that was dynamically selected and assigned to the ExamResult.
// This includes values selected through modifiers, order, etc.
func (er *ExamResult) Value(name string) (ent.Value, error) {
	return er.selectValues.Get(name)
}

// Update returns a builder for updating this ExamResult.
// Note that you need to call ExamResult.Unwrap() before calling this method if this ExamResult
// was returned from a transaction, and the transaction was committed or rolled back.
func (er *ExamResult) Update() *ExamResultUpdateOne {
	return NewExamResultClient(er.config).UpdateOne(er)
}

// Unwrap unwraps the ExamResult entity that was returned from a transaction after it was closed,
// so that all future queries will be executed through the driver which created the transaction.
func (er *ExamResult) Unwrap() *ExamResult {
	_tx, ok := er.config.driver.(*txDriver)
	if !ok {
		panic("ent: ExamResult is not a transactional entity")
	}
	er.config.driver = _tx.drv
	return er
}

// String implements the fmt.Stringer.
func (er *ExamResult) String() string {
	var builder strings.Builder
	builder.WriteString("ExamResult(")
	builder.WriteString(fmt.Sprintf("id=%v", er.ID))
	builder.WriteByte(')')
	return builder.String()
}

// ExamResults is a parsable slice of ExamResult.
type ExamResults []*ExamResult
