// Code generated by ent, DO NOT EDIT.

package ent

import (
	"common/ent/exam"
	"common/ent/generatedexam"
	"encoding/json"
	"fmt"
	"strings"
	"time"

	"entgo.io/ent"
	"entgo.io/ent/dialect/sql"
)

// GeneratedExam is the model entity for the GeneratedExam schema.
type GeneratedExam struct {
	config `json:"-"`
	// ID of the ent.
	ID int `json:"id,omitempty"`
	// IsActive holds the value of the "is_active" field.
	IsActive bool `json:"is_active,omitempty"`
	// RawExamData holds the value of the "raw_exam_data" field.
	RawExamData map[string]interface{} `json:"raw_exam_data,omitempty"`
	// CreatedAt holds the value of the "created_at" field.
	CreatedAt time.Time `json:"created_at,omitempty"`
	// UpdatedAt holds the value of the "updated_at" field.
	UpdatedAt time.Time `json:"updated_at,omitempty"`
	// Edges holds the relations/edges for other nodes in the graph.
	// The values are being populated by the GeneratedExamQuery when eager-loading is set.
	Edges               GeneratedExamEdges `json:"edges"`
	exam_generatedexams *int
	selectValues        sql.SelectValues
}

// GeneratedExamEdges holds the relations/edges for other nodes in the graph.
type GeneratedExamEdges struct {
	// Exam holds the value of the exam edge.
	Exam *Exam `json:"exam,omitempty"`
	// loadedTypes holds the information for reporting if a
	// type was loaded (or requested) in eager-loading or not.
	loadedTypes [1]bool
}

// ExamOrErr returns the Exam value or an error if the edge
// was not loaded in eager-loading, or loaded but was not found.
func (e GeneratedExamEdges) ExamOrErr() (*Exam, error) {
	if e.Exam != nil {
		return e.Exam, nil
	} else if e.loadedTypes[0] {
		return nil, &NotFoundError{label: exam.Label}
	}
	return nil, &NotLoadedError{edge: "exam"}
}

// scanValues returns the types for scanning values from sql.Rows.
func (*GeneratedExam) scanValues(columns []string) ([]any, error) {
	values := make([]any, len(columns))
	for i := range columns {
		switch columns[i] {
		case generatedexam.FieldRawExamData:
			values[i] = new([]byte)
		case generatedexam.FieldIsActive:
			values[i] = new(sql.NullBool)
		case generatedexam.FieldID:
			values[i] = new(sql.NullInt64)
		case generatedexam.FieldCreatedAt, generatedexam.FieldUpdatedAt:
			values[i] = new(sql.NullTime)
		case generatedexam.ForeignKeys[0]: // exam_generatedexams
			values[i] = new(sql.NullInt64)
		default:
			values[i] = new(sql.UnknownType)
		}
	}
	return values, nil
}

// assignValues assigns the values that were returned from sql.Rows (after scanning)
// to the GeneratedExam fields.
func (ge *GeneratedExam) assignValues(columns []string, values []any) error {
	if m, n := len(values), len(columns); m < n {
		return fmt.Errorf("mismatch number of scan values: %d != %d", m, n)
	}
	for i := range columns {
		switch columns[i] {
		case generatedexam.FieldID:
			value, ok := values[i].(*sql.NullInt64)
			if !ok {
				return fmt.Errorf("unexpected type %T for field id", value)
			}
			ge.ID = int(value.Int64)
		case generatedexam.FieldIsActive:
			if value, ok := values[i].(*sql.NullBool); !ok {
				return fmt.Errorf("unexpected type %T for field is_active", values[i])
			} else if value.Valid {
				ge.IsActive = value.Bool
			}
		case generatedexam.FieldRawExamData:
			if value, ok := values[i].(*[]byte); !ok {
				return fmt.Errorf("unexpected type %T for field raw_exam_data", values[i])
			} else if value != nil && len(*value) > 0 {
				if err := json.Unmarshal(*value, &ge.RawExamData); err != nil {
					return fmt.Errorf("unmarshal field raw_exam_data: %w", err)
				}
			}
		case generatedexam.FieldCreatedAt:
			if value, ok := values[i].(*sql.NullTime); !ok {
				return fmt.Errorf("unexpected type %T for field created_at", values[i])
			} else if value.Valid {
				ge.CreatedAt = value.Time
			}
		case generatedexam.FieldUpdatedAt:
			if value, ok := values[i].(*sql.NullTime); !ok {
				return fmt.Errorf("unexpected type %T for field updated_at", values[i])
			} else if value.Valid {
				ge.UpdatedAt = value.Time
			}
		case generatedexam.ForeignKeys[0]:
			if value, ok := values[i].(*sql.NullInt64); !ok {
				return fmt.Errorf("unexpected type %T for edge-field exam_generatedexams", value)
			} else if value.Valid {
				ge.exam_generatedexams = new(int)
				*ge.exam_generatedexams = int(value.Int64)
			}
		default:
			ge.selectValues.Set(columns[i], values[i])
		}
	}
	return nil
}

// Value returns the ent.Value that was dynamically selected and assigned to the GeneratedExam.
// This includes values selected through modifiers, order, etc.
func (ge *GeneratedExam) Value(name string) (ent.Value, error) {
	return ge.selectValues.Get(name)
}

// QueryExam queries the "exam" edge of the GeneratedExam entity.
func (ge *GeneratedExam) QueryExam() *ExamQuery {
	return NewGeneratedExamClient(ge.config).QueryExam(ge)
}

// Update returns a builder for updating this GeneratedExam.
// Note that you need to call GeneratedExam.Unwrap() before calling this method if this GeneratedExam
// was returned from a transaction, and the transaction was committed or rolled back.
func (ge *GeneratedExam) Update() *GeneratedExamUpdateOne {
	return NewGeneratedExamClient(ge.config).UpdateOne(ge)
}

// Unwrap unwraps the GeneratedExam entity that was returned from a transaction after it was closed,
// so that all future queries will be executed through the driver which created the transaction.
func (ge *GeneratedExam) Unwrap() *GeneratedExam {
	_tx, ok := ge.config.driver.(*txDriver)
	if !ok {
		panic("ent: GeneratedExam is not a transactional entity")
	}
	ge.config.driver = _tx.drv
	return ge
}

// String implements the fmt.Stringer.
func (ge *GeneratedExam) String() string {
	var builder strings.Builder
	builder.WriteString("GeneratedExam(")
	builder.WriteString(fmt.Sprintf("id=%v, ", ge.ID))
	builder.WriteString("is_active=")
	builder.WriteString(fmt.Sprintf("%v", ge.IsActive))
	builder.WriteString(", ")
	builder.WriteString("raw_exam_data=")
	builder.WriteString(fmt.Sprintf("%v", ge.RawExamData))
	builder.WriteString(", ")
	builder.WriteString("created_at=")
	builder.WriteString(ge.CreatedAt.Format(time.ANSIC))
	builder.WriteString(", ")
	builder.WriteString("updated_at=")
	builder.WriteString(ge.UpdatedAt.Format(time.ANSIC))
	builder.WriteByte(')')
	return builder.String()
}

// GeneratedExams is a parsable slice of GeneratedExam.
type GeneratedExams []*GeneratedExam
